<div class="m-5">
    <p>کاربر گرامی ! با سلام</p>
    <p>به سامانه پرتال خوش آمدید</p>
</div>
<div class="row" ng-show="notification.length">
    <div class="col-md-12">
        <div class="alert alert-secondary " role="alert" ng-repeat="row in notification">
            <button type="button" class="close" ng-click="readNotification(row.ID);" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <h4 class="alert-heading">{{row.Title}}</h4>
            <hr />
            <div ng-bind-html="row.Description"></div>
        </div>
    </div>
</div>



