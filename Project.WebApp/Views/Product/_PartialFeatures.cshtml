@model FM.Portal.Core.Model.Product
@{
    bool Negativefeatures = true;
    bool Positivefeatures = true;
}
<div class="container">
    <div class="row">
        <div class="col-md-6">
            <section class="card">
                <div class="card-header">
                    <span>
                        <i class="fa fa-check text-success"></i>
                        ویژگی های مثبت
                    </span>
                </div>
                <div class="card-body">
                    <ul class="list-group">
                        @if (Model.HasWater.HasValue && Model.HasWater.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>آب</li>
                        }
                        @if (Model.HasGas.HasValue && Model.HasGas.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>گاز</li>
                        }
                        @if (Model.HasElectricity.HasValue && Model.HasElectricity.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>برق</li>
                        }
                        @if (Model.HasElevator.HasValue && Model.HasElevator.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>آسانسور</li>
                        }
                        @if (Model.HasPhone.HasValue && Model.HasPhone.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>تلفن</li>
                        }

                        @if (Model.HasAirConditioning.HasValue && Model.HasAirConditioning.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>تهویه مطبوع</li>
                        }
                        @if (Model.HasBalcony.HasValue && Model.HasBalcony.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>بالکن</li>
                        }
                        @if (Model.HasCentralAntenna.HasValue && Model.HasCentralAntenna.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>آنتن مرکزی</li>
                        }
                        @if (Model.HasConferenceHall.HasValue && Model.HasConferenceHall.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>سالن کنفرانس</li>
                        }
                        @if (Model.HasGuard.HasValue && Model.HasGuard.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>نگهبان</li>
                        }
                        @if (Model.HasPhone.HasValue && Model.HasPhone.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>تلفن</li>
                        }
                        @if (Model.HasLobby.HasValue && Model.HasLobby.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>بالکن</li>
                        }
                        @if (Model.HasParking.HasValue && Model.HasParking.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>پارکینگ</li>
                        }
                        @if (Model.HasRemoteDoor.HasValue && Model.HasRemoteDoor.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>درب ریموت</li>
                        }
                        @if (Model.HasSportsHall.HasValue && Model.HasSportsHall.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>سالن ورزش</li>
                        }
                        @if (Model.HasSwimmingPool.HasValue && Model.HasSwimmingPool.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>استخر</li>
                        }
                        @if (Model.HasJacuzzi.HasValue && Model.HasJacuzzi.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>جکوزی</li>
                        }
                        @if (Model.HasSauna.HasValue && Model.HasSauna.Value)
                        {
                            Positivefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-success m-1"></i>سونا</li>
                        }
                        @if (Positivefeatures)
                        {
                            <li class="list-group-item">ندارد</li>
                        }
                    </ul>

                </div>
            </section>
        </div>
        <div class="col-md-6">
            <section class="card">
                <div class="card-header">
                    <span>
                        <i class="fa fa-times text-danger"></i>
                        ویژگی های منفی
                    </span>
                </div>
                <div class="card-body">
                    <ul class="list-group">
                        @if (!Model.HasWater.HasValue || !Model.HasWater.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-times text-danger m-1"></i>آب</li>
                        }
                        @if (!Model.HasGas.HasValue || !Model.HasGas.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-times text-danger m-1"></i>گاز</li>
                        }
                        @if (!Model.HasElectricity.HasValue || !Model.HasElectricity.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-times text-danger m-1"></i>برق</li>
                        }
                        @if (!Model.HasElevator.HasValue || !Model.HasElevator.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-times text-danger m-1"></i>آسانسور</li>
                        }
                        @if (!Model.HasPhone.HasValue || !Model.HasPhone.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-times text-danger m-1"></i>تلفن</li>
                        }


                        @if (!Model.HasAirConditioning.HasValue && !Model.HasAirConditioning.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>تهویه مطبوع</li>
                        }
                        @if (!Model.HasBalcony.HasValue && !Model.HasBalcony.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>بالکن</li>
                        }
                        @if (!Model.HasCentralAntenna.HasValue && !Model.HasCentralAntenna.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>آنتن مرکزی</li>
                        }
                        @if (!Model.HasConferenceHall.HasValue && !Model.HasConferenceHall.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>سالن کنفرانس</li>
                        }
                        @if (!Model.HasGuard.HasValue && !Model.HasGuard.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>نگهبان</li>
                        }
                        @if (!Model.HasPhone.HasValue && !Model.HasPhone.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>تلفن</li>
                        }
                        @if (!Model.HasLobby.HasValue && !Model.HasLobby.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>بالکن</li>
                        }
                        @if (!Model.HasParking.HasValue && !Model.HasParking.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>پارکینگ</li>
                        }
                        @if (!Model.HasRemoteDoor.HasValue && !Model.HasRemoteDoor.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>درب ریموت</li>
                        }
                        @if (!Model.HasSportsHall.HasValue && !Model.HasSportsHall.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>سالن ورزش</li>
                        }
                        @if (!Model.HasSwimmingPool.HasValue && !Model.HasSwimmingPool.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>استخر</li>
                        }
                        @if (!Model.HasJacuzzi.HasValue && !Model.HasJacuzzi.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>جکوزی</li>
                        }
                        @if (!Model.HasSauna.HasValue && !Model.HasSauna.Value)
                        {
                            Negativefeatures = false;
                            <li class="list-group-item"><i class="fa fa-check text-danger m-1"></i>سونا</li>
                        }

                        @if (Negativefeatures)
                        {
                            <li class="list-group-item">ندارد</li>
                        }
                    </ul>

                </div>
            </section>
        </div>
    </div>
</div>