@section BannerSection{
    @{Html.RenderAction("RenderSlide", "Home");}
    }

@section BodySection{
    <div class="row">
        <div class="col-lg-12 mt-2">
            @{Html.RenderAction("HomeProduct", "Home");}
            @{Html.RenderAction("ShopProduct", "Home");}
        </div>
    </div>
}